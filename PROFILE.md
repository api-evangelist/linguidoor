# Linguidoor

Berlin-based human translation and localization agency serving businesses and individuals across Germany and internationally — professional translation in 100+ languages, website / app / software / game localization, MTPE, certified and sworn document translation for German administrative processes, interpretation, subtitling, voice-over and dubbing, transcription, DTP, content writing, international SEO, and AI data services (annotation, labeling, voice/text data collection, TTS and voice cloning).

- **Provider:** https://linguidoor.com
- **Documentation:** — (no developer documentation exists)
- **Base URL:** — (no API host)
- **Tags:** Translation Services, Localization, Language Services, Interpretation, Document Translation, Certified Translation, Content Services, Media / Dubbing, Transcription, AI Data Services, Professional Services, Germany
- **Public API:** no
- **Agent-native (MCP / llms.txt / skills):** llms.txt only — and it is empty of business content
- **Enrichment pass:** 2026-08-11 (`local-v1`)

## APIs

- **Linguidoor Agent Discovery** — the only agent-native artifact. `https://linguidoor.com/llms.txt` returns HTTP 200, `text/plain`, 225 bytes, and its entire body is one link to the default WordPress "Hello world!" post, footed with `[comment]: # (Generated by Hostinger Tools Plugin)`. It describes none of Linguidoor's services, pricing or shop.

## What contract discovery found

Every STEP 0b probe was run against `linguidoor.com` (the only host in the profile):

| Probe | Result |
|---|---|
| `/openapi.json`, `/openapi.yaml`, `/swagger.json`, `/api-docs`, `/apis.json` | 404 (WordPress 404 page, `text/html`) |
| `/graphql` (introspection) | 404 |
| `/mcp`, `/sse`, `/.well-known/mcp.json` | 404 |
| `/.well-known/agent-card.json`, `/.well-known/agent.json` | 404 — **no agent card written** |
| `/.well-known/security.txt`, `openid-configuration`, `oauth-authorization-server`, `oauth-protected-resource`, `api-catalog`, `ai-plugin.json` | 404 — **no WellKnown pointer emitted** |
| `/api`, `/docs`, `/developers`, `/status`, `/changelog`, `status.linguidoor.com`, `trust.linguidoor.com` | 404 / no host |
| npm, PyPI, RubyGems, crates.io, Packagist, `github.com/linguidoor` | no packages, no GitHub org |
| `/llms.txt` | **200** — harvested verbatim to `llms/linguidoor-llms.txt` |
| `/wp-json/` | **200** — WordPress REST API discovery index, 26 namespaces / 831 routes |

The site runs WordPress + WooCommerce on Hostinger. `/wp-json/` exposes the **platform-default** REST API, including the public unauthenticated **WooCommerce Store API** (`/wp-json/wc/store/v1`) that returns the 19 fixed-price certified-translation products. That is a CMS default, **not** a Linguidoor developer program, so it is recorded as evidence rather than cataloged as an API contract.

## Artifacts in this repo

- `plans/linguidoor-plans-pricing.yml` — 3 published per-word service tiers (€0.05 / €0.08 / €0.14 per word) plus 19 fixed-price certified document translations read from the Store API.
- `rate-limits/linguidoor-rate-limits.yml` — `limit_count: 0`; a live unauthenticated request returned no `RateLimit-*`, `X-RateLimit-*` or `Retry-After` header.
- `well-known/linguidoor-well-known.yml` — the full probe record. Every `/.well-known/` path 404s; a recorded absence.
- `security/linguidoor-domain-security.yml` — TLS 1.3, cert to 2026-10-18, **no HSTS**, no DNSSEC, no CAA; SPF present, DMARC present at `p=none`.
- `llms/linguidoor-llms.txt` — the served llms.txt, verbatim.

No vulnerability-disclosure program and no trust center were found (`probe-security-programs.py`: `vdp=none trust=none`).

_Enriched by the API Evangelist enrichment pipeline from Linguidoor's own public surface._
